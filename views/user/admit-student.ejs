<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=pageTitle%></title>
    <link rel="stylesheet" href="/css/admit-student.css" />
    <link rel="stylesheet" href="/css/header.css" />
  </head>

  <body>
    <%-include('../includes/navigation.ejs')-%>
    <div class="container">
      <h1>HIRA SECONDARY SCHOOL AND COLLEGE</h1>
      <h2>Student Registration From</h2>
      <div class="form-wrapper">
        <form
          action="<%=editing?'/admin/edit-student':'/admit-student'%>"
          method="post"
          name="admissionForm"
        >
          <div class="form-item">
            <label for="fullname">Student Name:</label>
            <input
              type="text"
              name="name"
              id="fullname"
              placeholder="Full Name"
              required
              value="<%=editing?data.name:''%>"
            />
          </div>
          <div class="form-item">
            <label for="username">Father's Name:</label>
            <input
              type="text"
              name="fatherName"
              id="fathersname"
              placeholder="Father's Full Name"
              required
              value="<%=editing?data.fathername:''%>"
            />
          </div>

          <div class="form-item">
            <label for="gender">Gender:</label>
            <div class="genders">
              <p>Male</p>
              <input
                type="radio"
                name="gender"
                id="gender"
                value="male"
                required
                <% if(editing){ %>
                <%if(data.gender==='male'){%>
                  checked
               <% }%>
              <%}%>
              />
              <p>Female</p>
              <input type="radio" name="gender" id="gender" value="female" 
             <% if(editing){ %>
                <%if(data.gender==='female'){%>
                  checked
               <% }%>
              <%}%>
              
              />
              <p>Other</p>
              <input type="radio" name="gender" id="gender" value="other" 
               <% if(editing){ %>
                <%if(data.gender==='other'){%>
                  checked
               <% }%>
              <%}%>
              
              />
            </div>
          </div>
          <div class="form-item">
            <label for="email">Date of Birth</label>
            <input
              type="date"
              name="dateOfBirth"
              id="DOB"
              required
              value="<%=editing?data.dateOfBirth:''%>"
            />
          </div>
          <div class="form-item">
            <label for="email">E-mail:</label>
            <input
              type="email"
              name="email"
              id="email"
              placeholder="email@xyz.com"
              required
              value="<%=editing?data.email:''%>"
            />
          </div>
          <div class="form-item">
            <label for="email">Address:</label>
            <input
              type="text"
              name="address"
              value="<%=editing?data.address:''%>"
            />
          </div>
          <%if(editing){%>
          <input type="hidden" value="<%=data.id%>" name="id" />
          <%}%>

          <hr />

          <button class="register" type="submit"><%= editing?"Edit Student":"Register"%></button>
        </form>
      </div>
    </div>
  </body>
</html>
